dvcs-d:process:
  stage: process
  extends: .phy_benchmark
  tags:
    - phy
  needs: ["common:detector"]
  script:
    - compile_analyses.py dvcs-d
    - bash benchmarks/dvcs-d/dvcs-d.sh --all

dvcs-d:results:
  stage: collect
  needs: ["dvcs-d:process"]
  script:
    - ls -lrth
